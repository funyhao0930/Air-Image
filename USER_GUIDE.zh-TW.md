# Gemini 2 空中虛擬鍵盤：保母級操作指南

這份指南是寫給第一次使用本專案、第一次操作 Orbbec Gemini 2，甚至不熟悉 PowerShell 的使用者。請按照順序做，不要跳步；每個步驟都會告訴你「應該看到什麼」。

> 這是 MVP 原型。程式目前只會在 OpenCV 視窗和 PowerShell 顯示按鍵事件，不會把數字真的輸入到記事本、瀏覽器或其他 Windows 程式。

## 先看懂：你最後要完成什麼

完整流程只有四件事：

1. 讓電腦看見 Gemini 2。
2. 從正確的輸出資料夾啟動程式。
3. 用 `C`、表面掃描和三個校正點告訴程式「虛擬鍵盤在哪裡、大小是多少」。
4. 用食指靠近平面，讓程式產生按鍵事件。

第一次使用時，請把「校正」當成必要步驟。程式重新啟動後，三點校正結果不會保留，必須重新校正。

## 第一部分：啟動前先準備好

### 1. 硬體擺放

請先做以下準備：

1. 將 Orbbec Gemini 2 接上電腦。
2. 優先使用主機板直連的 USB 3.x 連接埠，不要先接 USB Hub。
3. 將相機固定好，避免操作時碰到相機或拉扯 USB 線。
4. 相機前方留出手指可以伸出、移動、靠近平面的空間。
5. 保持光線穩定；避免指尖反光、過暗，或被其他物體遮住。
6. 如果有開啟 Orbbec Viewer 或其他會使用相機的程式，先關閉它們。

如果相機在移動或傾斜時出現 `Device changed! removed`，先停下來檢查 USB 線、接頭、Hub 和供電，不要先把問題當成程式碼錯誤。

### 2. 準備一張紙本鍵盤樣板（強烈建議）

校正只需要三個點，但這三個點必須指得準。在空白桌面上「想像」三個位置，每次校正都會落在不同的地方；在紙上把它們畫出來，你每次都指同樣的三個十字。

請這樣做：

1. 用列印或手繪，在一張白紙上畫一個 3 欄、4 列的方框，整體大約 **90 × 120 mm**（每一格約 30 × 30 mm）。
2. 在方框內依序寫上 `1`～`9`，第四列只在**中間那一格**寫 `0`，左右兩格留白。
3. 在三個校正位置各畫一個明顯的十字或圓點，並標上 `P1`、`P2`、`P3`：
   - `P1`：方框的左上角，也就是 `1` 鍵的左上角。
   - `P2`：方框的右上角，也就是 `3` 鍵的右上角。
   - `P3`：方框的下邊界，`0` 鍵正下方。
4. 把紙平放在相機看得到的桌面上，四角壓平，不要拱起或垂在桌緣。

這個尺寸有兩個好處：`P2`、`P3` 距離 `P1` 都遠超過預設要求的 80 mm，不會觸發「校正點太接近」；而每一格 30 mm 對指尖來說夠大，按起來不容易越界。

紙張只是「瞄準用的標記」，程式並不認得紙上的圖案。**校正完成後如果你不想看到紙，可以把它拿走**——只要相機沒有被碰到、桌面高度也沒有改變，已經算好的鍵盤幾何仍然有效。但只要相機或桌面移動過，就必須重新校正。

### 3. 你需要的軟體

建置程式需要以下環境：

| 軟體 | 用途 |
| --- | --- |
| Windows 11 | 執行平台。 |
| Visual Studio 2026 Community 或相容的 MSVC Build Tools | 編譯 C++ 程式。 |
| CMake、Ninja | 設定與建置專案。 |
| Bazelisk | 建置 MediaPipe bridge。 |
| MSYS2 | 提供 MediaPipe 建置所需的 `bash` 與 `awk`。 |
| Orbbec SDK 2.9.3 | 連接 Gemini 2；預設位置是 `C:\Program Files\OrbbecSDK 2.9.3`。 |

OpenCV 和 yaml-cpp 會由本專案的 vcpkg manifest 管理。

### 4. 你要在哪裡輸入指令

本指南假設專案位於：

```text
D:\Claude Project\Air Image
```

如果你的專案放在別的資料夾，以下指令中的路徑請換成你的實際位置。

最簡單的做法是：在檔案總管打開專案資料夾，按住 `Shift` 後在空白處按滑鼠右鍵，選擇「在終端機中開啟」。

也可以開啟 Visual Studio 的 **Developer PowerShell for VS 2026**，再執行：

```powershell
Set-Location -LiteralPath "D:\Claude Project\Air Image"
```

執行後，提示字元最後應該會顯示類似：

```text
PS D:\Claude Project\Air Image>
```

如果你看到的是其他資料夾，不要急著執行建置指令；先重新執行上面的 `Set-Location`。

## 第二部分：如果你已經有建置好的程式

如果 `build\windows-release` 已經存在，而且裡面有完整 runtime，可以直接跳到[第四部分：啟動程式](#第四部分啟動程式)。

### 先檢查輸出檔案

在專案根目錄執行：

```powershell
$required = @(
  ".\build\windows-release\aerial_touch_app.exe",
  ".\build\windows-release\OrbbecSDK.dll",
  ".\build\windows-release\OrbbecSDKConfig.xml",
  ".\build\windows-release\extensions\frameprocessor\ob_frame_processor.dll",
  ".\build\windows-release\mediapipe_hand_bridge.dll",
  ".\build\windows-release\assets\models\hand_landmarker.task",
  ".\build\windows-release\config\default.yaml"
)
$required | ForEach-Object {
  [pscustomobject]@{ Exists = Test-Path -LiteralPath $_; Path = $_ }
} | Format-Table -AutoSize
```

每一列的 `Exists` 都應該是 `True`。如果 `mediapipe_hand_bridge.dll` 是 `False`，請不要直接啟動；請按照下一部分重新建置 bridge。

## 第三部分：第一次建置程式

如果你只是要啟動既有輸出，可以略過本部分。如果這是第一次使用，請完整做完本部分。

### 1. 確認工具能被找到

在專案根目錄逐行執行：

```powershell
cmake --version
ninja --version
bazelisk --version
```

每一行都應該顯示版本號。如果顯示「不是內部或外部命令」，先安裝對應工具，或改用 Visual Studio Developer PowerShell，讓 Visual Studio 的編譯工具加入目前環境。

### 2. 先建立 CMake 輸出與 vcpkg 相依套件

```powershell
cmake --preset windows-release
```

成功時不應出現 `CMake Error`，並會建立：

```text
build\windows-release
```

這一步先做，不要跳過；MediaPipe bridge 建置需要這一步產生的 OpenCV 檔案。

### 3. 取得固定版本的 MediaPipe

如果 `third_party\mediapipe` 尚未存在，執行：

```powershell
git clone --depth 1 --branch v0.10.35 https://github.com/google-ai-edge/mediapipe.git third_party/mediapipe
```

如果資料夾已經存在，不要再次 `git clone`；直接進行下一步。

### 4. 下載並驗證手部模型

```powershell
.\scripts\fetch_hand_model.ps1
```

成功時會顯示已驗證的 `hand_landmarker.task` 路徑。這個腳本會同時檢查 SHA-256；如果出現 checksum mismatch，先保留錯誤訊息，不要自行下載其他模型替代。

### 5. 建置 MediaPipe bridge

```powershell
.\scripts\build_mediapipe_bridge.ps1 -MediaPipeSource .\third_party\mediapipe
```

這一步可能比一般 CMake 建置久。腳本會檢查 MediaPipe v0.10.35、套用 Windows 相容補丁，最後把 bridge 和必要 DLL 複製到 `build\windows-release`。

成功後，至少應看到：

```text
build\windows-release\mediapipe_hand_bridge.dll
build\windows-release\opencv_core4.dll
build\windows-release\opencv_imgproc4.dll
build\windows-release\z.dll
```

### 6. 編譯主程式

```powershell
cmake --build --preset windows-release
```

成功後應有：

```text
build\windows-release\aerial_touch_app.exe
build\windows-release\orbbec_stream_probe.exe
```

CMake 也會把 Orbbec SDK 的 DLL、設定檔和完整 `extensions` 目錄部署到輸出資料夾。請不要只把 EXE 單獨複製到另一個資料夾。

### 7. 執行測試

```powershell
ctest --preset windows-release --output-on-failure
```

測試成功時會顯示所有測試通過。若 PowerShell 找不到 `ctest`，請改用 Visual Studio Developer PowerShell，或確認 CMake 已加入 PATH；不要把「沒有執行測試」當成「測試通過」。

## 第四部分：啟動程式

### 建議方式：從輸出資料夾啟動

在專案根目錄執行：

```powershell
Set-Location -LiteralPath ".\build\windows-release"
.\aerial_touch_app.exe
```

這是最不容易出錯的方式。程式預設會從目前資料夾尋找：

```text
config\default.yaml
mediapipe_hand_bridge.dll
assets\models\hand_landmarker.task
```

### 從專案根目錄直接啟動

如果你不想切換資料夾，可以使用完整相對路徑：

```powershell
.\build\windows-release\aerial_touch_app.exe `
  --config .\build\windows-release\config\default.yaml `
  --bridge .\build\windows-release\mediapipe_hand_bridge.dll `
  --model .\build\windows-release\assets\models\hand_landmarker.task
```

### 啟動成功時你應該看到什麼

1. PowerShell 不會立刻回到提示字元，而是持續輸出程式訊息。
2. 出現名為 `Gemini 2 空中鍵盤` 的 OpenCV 視窗。
3. 視窗中有 Gemini 2 的彩色影像。
4. 視窗左上方顯示多行狀態文字，其中一行是操作提示：`C：校正 | S：參數 | 空白鍵：開始掃描或取樣 | Enter：完成校正 | R：重設 | Q/Esc：離開`。
5. PowerShell 會列出目前的對齊方式、深度模式、深度精度與 FPS，以及 Gemini 2 回報的可用設定清單。
6. 狀態列顯示 `狀態：按 C 開始設定數字鍵盤範圍`。

此時還看不到數字鍵盤；鍵盤要等校正完成後才會畫出來。

第一次啟動時沒有看到手是正常的；先把手放進畫面，下一部分會說明如何判斷是否真的正常。

> 不建議直接雙擊 EXE。若程式立即結束，雙擊視窗可能一閃就消失，你會看不到錯誤原因。請從 PowerShell 啟動。

## 第五部分：啟動後先確認相機和手部追蹤

### 1. 確認手部位置

把整隻手放進相機畫面，讓食指伸直。不要只伸出一小截手指，也不要讓食指被手掌、桌面或其他物體遮住。

### 2. 看主視窗左上方的狀態

目前主視窗會顯示下列資訊：

| 顯示內容 | 代表什麼 | 第一次使用時怎麼做 |
| --- | --- | --- |
| `主程式 FPS` | 主程式畫面更新速度。 | 持續有數字即可；實機驗收目標至少 15 FPS。 |
| `相機：... FPS ...` | 相機實際使用的 FPS、深度模式和深度精度。 | 先記下來，通常不用修改。 |
| `對齊：硬體 D2C` 或 `軟體 D2C` | RGB 和深度影像如何對齊。 | 兩者都可以工作。 |
| `追蹤：本幀有效` | 這一幀的指尖位置可用於校正和觸控。 | 看到它再開始校正。 |
| `追蹤：未取得本幀觀測` | 目前沒有可確認的手部觀測。 | 調整手的位置、光線和食指姿勢。 |
| `追蹤：無法使用` | MediaPipe bridge 或模型沒有載入成功。 | 先看[故障排除](#第十部分故障排除照症狀處理)。 |
| `指尖像素（濾波）` | One Euro Filter 後的指尖像素位置；只有本幀有效時才可操作。 | 看到穩定標記即可。 |
| `XYZ` | 指尖三維座標，單位 mm。 | 校正時要有這一行。 |
| `平面 UV` | 指尖投影到校正平面的位置。 | 完成校正後才會出現。 |
| `距離` | 濾波後與原始的平面距離，單位 mm，同一行還會顯示目前的 `按鍵`。 | 觸控判定使用濾波值。 |
| `觸控：可觸發` 或 `等待手指離開` | 目前能不能再產生一次按鍵事件。 | 要能按鍵，必須先顯示 `可觸發`。 |
| `接近速度已達標` | 這一次接近已經通過最低接近速度閘門。 | 出現後只要進入觸控門檻就會觸發。手指回退超過 3 mm，或已經進到觸控門檻內卻沒有觸發，這個標記就會消失，必須重新接近。 |
| `停留 N/350 ms` | 指尖已經在同一個鍵的觸控門檻內停留多久。 | 數字滿了就會用停留觸發。 |
| `深度：指節外推` 或 `單點探測` | 指尖距離是怎麼量到的。 | 兩者都正常，不用調整。 |
| `深度：探測失敗` | 這一幀在指節附近讀不到有效深度。 | 偶爾出現正常；持續出現請看 [E](#e-看到指尖但沒有-xyz-或無法開始取樣)。 |
| `校正：... N/3` | 校正目前的階段，以及已記錄幾個校正點。 | 校正時盯著這一行。 |
| `狀態：...` | 程式對你下一步的指示。 | 每一步都先讀這一行再動作。 |
| `相機警告：...` | 相機設定或內參有問題。 | 見[故障排除](#第十部分故障排除照症狀處理) I 項。 |

如果你看到手部骨架、食指指尖標記，以及會更新的 `XYZ`，表示可以進入校正。

## 第六部分：`S` 參數設定視窗

第一次使用建議先保留預設值。只有在已完成一次正常校正、知道自己要調整什麼時，才使用 `S`。

### 開啟和關閉

1. 確認焦點在 `Gemini 2 空中鍵盤` 主視窗。
2. 按 `S`。
3. 會出現「參數設定」視窗；再按一次 `S` 可隱藏它。

### 視窗裡可以調整什麼

| 設定 | 單位 | 白話意思 |
| --- | --- | --- |
| 觸控門檻 | mm | 指尖靠近到這個距離以內，才可能觸發。 |
| 離開門檻 | mm | 指尖離開到這個距離以外，才重新允許下一次觸發。 |
| 最低接近速度 | mm/s | 指尖必須以足夠的速度靠近，避免慢慢漂移造成誤觸。 |
| 追蹤逾時 | ms | 手部追蹤消失多久後，程式才清除暫存狀態。 |
| 深度取樣半徑 | px | 取得指尖深度時使用的影像鄰近範圍。 |
| 校正點最小距離 | mm | 三個鍵盤邊界點中，P2 和 P3 至少要離 P1 多遠。 |
| 深度工作模式 | 下拉選單 | Gemini 2 實際支援的深度模式。 |
| 深度精度 | 下拉選單 | Gemini 2 實際支援的深度精度。 |
| FPS | 下拉選單 | RGB 和深度能配對使用的 FPS。 |

相機的三個下拉選單只會列出 Gemini 2 目前回報支援的選項，不要自行輸入不存在的模式名稱。

### 修改設定的正確順序

1. 用滑桿調整，或在數值框輸入數字。
2. 確認沒有紅色錯誤提示。
3. 確認離開門檻大於觸控門檻。
4. 按「套用並儲存」。
5. 若只想放棄修改，按「取消」或關閉視窗。

「恢復預設值」只是把預設值載入表單，還不會寫入設定檔；要真的保存，仍要按「套用並儲存」。

觸控、深度取樣等互動設定會立即套用；深度模式、深度精度和 FPS 會顯示「重新啟動後生效」，必須關閉程式再重新啟動。校正點和鍵盤尺寸不會在這裡輸入，而是由三點校正自動計算。

### 預覽區怎麼看

設定視窗右側的「觸控距離預覽」會顯示：

- 目前距離是否有效。
- 目前是否偵測到手。
- 指尖目前對應的按鍵。
- `可觸發` 或 `等待手指離開`。
- 觸控門檻和離開門檻。

### 只能在 YAML 修改的進階設定

以下欄位不在設定視窗裡，只能編輯 `config\default.yaml`，改完要重新啟動程式。如果你不熟悉這些名詞，先不要修改。

| YAML 欄位 | 預設值 | 白話意思 |
| --- | --- | --- |
| `touch.dwell_ms` | 350 | 指尖停在同一個鍵的觸控門檻內達到這個毫秒數，就直接送出按鍵，不必再滿足最低接近速度。設為 `0` 就完全關閉停留觸發，回到只認「靠近速度」的舊行為。 |
| `depth.surface_scan_radius_px` | 150 | 表面掃描的取樣半徑（像素）。這個範圍必須涵蓋鍵盤的相當比例：如果只掃到一條細帶，程式雖然能貼合出一個平面，但那個平面的傾角其實沒有被決定，投影出來的鍵盤就會歪掉。 |
| `depth.surface_scan_stride_px` | 5 | 取樣間隔（像素）。調小會更密但更吃效能。 |
| `depth.hand_exclusion_px` | 40.0 | 距離任一手部關鍵點這麼近的像素一律不當成表面。手掌貼在目標上時和目標幾乎一樣深，只靠深度分不出來。 |
| `depth.surface_depth_window_mm` | 150.0 | 比指尖再遠超過這個距離的樣本算背景，不是目標表面。 |
| `depth.finger_clearance_mm` | 3.0 | 表面掃描時，比指尖再近超過這個距離的深度樣本會被當成手指丟掉。指尖在畫面上只有十幾個像素寬，取樣環很容易打在手指本身；手指從指尖往後會抬離桌面，所以手指像素比指尖更靠近相機，而桌面像素與指尖同深或更遠。因為判斷基準落在指尖前方而不是後方，你把手指輕輕貼著桌面掃描時一樣收得到樣本。 |
| `fingertip.depth_probe_near_ratio` | 0.35 | 深度探測點落在「指尖 → 食指 DIP 關節」連線上的比例（靠近指尖的那一個）。指尖本身位於手指輪廓邊緣，深度會把手指和桌面混進同一個 ROI，所以距離改從手指實心的部位量。 |
| `fingertip.depth_probe_far_ratio` | 0.70 | 同一條連線上比較靠近關節的第二個探測點比例。兩個探測點的深度差就是外推回指尖時使用的梯度。 |

其他 RGB 防閃爍、SDK 濾波器、One Euro 濾波、深度跳值、按鍵邊界遲滯、校正樣本數等欄位也同樣只在 `config\default.yaml` 管理。

## 第七部分：三點校正——最重要的操作

### 先理解三個點是什麼

校正只要三個點。它們不是要按三次鍵，也不是點數字的中心；它們是用食指指向「虛擬鍵盤外框的三個位置」，讓程式量出平面方向和鍵盤的整體大小。

三個點的意思是：

```text
P1 ●────────┬────────┬────────● P2
   │   1    │   2    │   3    │
   ├────────┼────────┼────────┤
   │   4    │   5    │   6    │
   ├────────┼────────┼────────┤
   │   7    │   8    │   9    │
   ├────────┼────────┼────────┤
   │        │   0    │        │
   └────────┴───●────┴────────┘
                P3
```

| 點 | 要指的位置 | 這個點用來量什麼 |
| --- | --- | --- |
| P1 | `1` 鍵左上角 | 鍵盤原點。 |
| P2 | `3` 鍵右上角 | 整張鍵盤的上邊界方向與總寬度。 |
| P3 | `0` 鍵正下方的鍵盤下邊界 | 整張鍵盤的總高度；不是右下角。 |

有了這三個點，外框就完全確定了。程式接著把外框直接除成 3 欄 × 4 列，每一格就是一個按鍵；第四列只有中間那一格是 `0`，左右兩格沒有對應按鍵。

### 為什麼從七個點減成三個點

舊版要指七個點：除了上面三個，還要再指四個角點，分別量「單鍵寬」、「單鍵高」、「水平間距」和「垂直間距」。新版把它們全部拿掉，理由對你有直接好處：

- **要瞄準的目標少了四個**：校正時間遠不到原本的一半，手也不必在半空中撐那麼久。
- **按鍵之間不再有空隙**：外框除以 3 × 4 之後，鍵與鍵完全相連。舊版量出來的「間距」會在鍵與鍵之間留下一條沒有按鍵的死區；在半空中指東西本來就有幾毫米誤差，只要落進那條死區，畫面上的 `按鍵` 就會變成 `-`，你明明按下去卻什麼都不會發生。現在同樣的幾毫米誤差最多只是按到隔壁鍵，你馬上就看得出來。
- **失敗機會變少**：舊版那四個點還要彼此交叉檢查，任何一項對不起來就整組作廢，得從頭再來。

所以校正完成後，畫面不會再報告間距，而是直接標示「按鍵相連無間隙」。

### 開始校正

1. 先確認畫面顯示 `追蹤：本幀有效`；看到 `XYZ` 代表指尖深度也正常。
2. 如果你做了紙本樣板，先把它放好、壓平。
3. 按 `C`。狀態會變成 `先沿預計的鍵盤區域移動食指，按空白鍵開始表面掃描`。
4. 將食指輕碰或靠近目標表面，沿預計的鍵盤範圍慢慢移動。
5. 按一次 `Space` 開始表面掃描，然後**把整個鍵盤區域都掃過一遍，特別是四個角落**。掃描會自動完成，不要立刻按 `Enter`。
6. 狀態列的 `涵蓋範圍` 會即時顯示已經掃到多寬，要達到門檻掃描才會完成。只在中央小範圍來回移動是不夠的：那樣程式雖然貼合得出一個平面，但平面的傾角並沒有被決定，投影出來的鍵盤就會歪掉。

正常時會看到類似：

```text
表面掃描中：請沿鍵盤區域移動食指，完成後會自動開始 P1 取樣
```

接著會持續更新收集進度：

```text
表面掃描中：已收集 ... 個深度樣本；請沿鍵盤區域移動食指
```

掃描完成後，畫面會顯示：

```text
表面掃描完成；將手指移到 1 鍵左上角並按空白鍵開始取樣
```

這時才移到 P1，開始三點取樣。掃描時，程式會讀取指尖周邊未被遮住的目標深度；紙張、板面或螢幕周邊必須在相機深度畫面中可見。

### 每一個點都用同一個小流程

對 P1、P2、P3，每個點都重複以下五步：

1. 把食指尖端放到指定位置。
2. 讓手指完全停住，不要邊移動邊按鍵。
3. 按一次 `Space`，開始收集這個位置的樣本。
4. 看到 `校正點取樣：.../18，請保持不動` 後，繼續保持不動。
5. 等畫面顯示 `已記錄` 和下一個位置後，才移到下一點。

預設每個點需要 18 筆有效樣本。如果你曾在 YAML 修改樣本數，畫面上的實際數字可能不同；以畫面顯示為準。參數設定視窗目前不能修改樣本數。

取樣期間不要再按 `Space`，也不要按 `Enter`。取樣完成後，程式會自動記錄該點，並在畫面最下方更新一行散布資訊：

```text
校正散布（最大軸）：P1 ... / P2 ... / P3 ... mm
```

這一行是每個點在取樣期間的抖動量，數字越小越穩。三個點的散布會擠在同一行顯示，不會佔滿畫面。

### 按照順序完成三個點

現在照著下面的口令做，一次只做一點。

#### P1：1 鍵左上角

把指尖放在 `1` 鍵左上角（樣板上標 `P1` 的十字），按 `Space`，保持不動直到畫面顯示：

```text
已記錄 1 鍵左上角；請移到 3 鍵右上角並按空白鍵開始取樣
```

#### P2：3 鍵右上角

把指尖水平移到 `3` 鍵右上角，按 `Space`，保持不動直到畫面顯示：

```text
已記錄 3 鍵右上角；請移到 0 鍵正下方並按空白鍵開始取樣
```

#### P3：0 鍵正下方

把指尖移到 `0` 鍵正下方的鍵盤下邊界，按 `Space`，保持不動直到畫面顯示：

```text
已記錄 0 鍵正下方；請按 Enter 計算鍵盤尺寸並完成校正
```

這一步最容易做錯：P3 不是鍵盤右下角，也不是 `0` 鍵右下角。它左右的位置很寬鬆，真正重要的是它「離上邊界多遠」——那就是整張鍵盤的高度。

### 完成三點校正

P3 完成後，畫面會提示按 `Enter`。確認：

1. `校正：進行中 3/3`。
2. 沒有顯示「校正點正在取樣」。
3. 手指目前仍在 P3 附近即可；解算時不需要再按 `Space`。

三個點都記錄完之後再按 `Space` 不會有任何作用，請直接按 `Enter`。

成功時，狀態會顯示類似：

```text
校正完成：鍵盤 ... × ... mm；按鍵 ... × ... mm（按鍵相連無間隙）
```

此時：

- `校正` 會變成完成。
- 視窗右下角會出現固定版面的數字鍵盤指示。
- **畫面中的實體表面上會直接畫出 10 個按鍵方框**。
- 程式已根據三點自動計算鍵盤總寬高與單鍵寬高。

### 一定要做的最後檢查：方框有沒有落在實體目標上

畫在影片裡的那組按鍵方框，是把校正後的鍵盤角點用「和觸控距離同一組相機模型」反投影回畫面畫出來的。所以它是一面免費的照妖鏡：

- **方框和你的紙本樣板（或桌面標記）重合** → 校正正確，可以開始按鍵。
- **方框明顯歪掉、整體平移或大小不對** → 先不要相信任何毫米數字。這通常代表對齊後的深度內參與影像尺寸不符，程式會在畫面上出現 `相機警告：深度內參尺寸與對齊後影像不符...`，並在 PowerShell 印出同樣的訊息。這種情況下每一個毫米讀數都會被縮放錯，觸控門檻也跟著失準。

如果只是稍微偏一點點，通常是三個點指得不夠準；按 `C` 重新校正一次通常就會改善。

### 校正失敗怎麼辦

如果按 `Enter` 後失敗，程式會清除三個點但保留已驗證的表面掃描，並顯示失敗原因加上：

```text
；表面基準已保留，請從 1 鍵左上角重新取樣
```

請直接從 P1 重新開始。只有相機、紙張或鍵盤所在表面已移動時，才按 `C` 重新掃描。三種失敗訊息的意思見[故障排除](#第十部分故障排除照症狀處理) H 項。

## 第八部分：實際按數字鍵

### 第一次測試只按一個鍵

校正完成後，先不要快速連按。只測試一個鍵，例如 `5`：

1. 將食指移到**畫在實體表面上**的 `5` 鍵方框附近。畫面右下角那組固定鍵盤只是狀態指示，不是你要瞄準的地方。
2. 觀察畫面的 `按鍵` 是否顯示 `5`。
3. 如果顯示 `-`，代表指尖不在鍵的有效範圍內，先調整左右或上下位置。
4. 確認 `按鍵：5` 後，把手指移到虛擬平面上方。
5. 以清楚、穩定的動作朝平面靠近。
6. 看到 `最近按鍵：5`，或在 PowerShell 看到按鍵事件，就代表第一次測試成功。

### 有兩種觸發方式

程式現在有兩條路可以送出按鍵，任一條成立就會觸發：

1. **接近觸發**：從平面上方以足夠的速度靠近，進入觸控門檻。整段接近過程中只要曾經達到最低接近速度就算數，畫面會顯示 `接近速度已達標`。
2. **停留觸發**：指尖進入觸控門檻後，停在**同一個鍵**上不動達到 `touch.dwell_ms`（預設 350 ms）就會送出按鍵。畫面會顯示 `停留 N/350 ms` 的倒數進度。

第二條路是專門為了慢慢按的人加的。舊版只有接近觸發，慢速、小心翼翼地把手指放下去會被速度閘門判定為不合格，然後**靜默地被丟掉**——畫面沒有任何提示，你只會覺得「明明按了卻沒反應」。現在只要你願意多停 0.35 秒，慢按也一定會出來。

停留觸發有兩個要記住的行為：

- **滑到別的鍵會重新計時**。停留是綁在單一個鍵上的；手指從 `5` 滑到 `6`，`6` 的計時從零開始，不會一路拖出一整串按鍵。
- **停著不會自動連發**。觸發過一次之後，即使你繼續停在同一個鍵上也不會再出第二次；必須先把手指退到離開門檻以外，才會重新 `可觸發`。

如果你不想要停留觸發，把 `config\default.yaml` 的 `touch.dwell_ms` 改成 `0` 就會完全關閉，回到只認接近速度的行為。

### 為什麼不能一直停在鍵上

不論用哪一種方式觸發，re-arm 規則都一樣：

1. 第一次靠近或停留，產生一次事件。
2. 手指停在同一個鍵上，不會重複產生事件。
3. 要再按一次，必須先把手指移遠，直到 `觸控：可觸發`。
4. 再次靠近平面，或再次停留滿時間，才會產生下一次事件。

### 顏色與畫面提示

- 數字鍵顯示黃色或橘黃色：指尖目前懸停在該鍵。
- 數字鍵顯示綠色：剛剛產生了按鍵事件。
- `觸控：可觸發`：可以進行下一次靠近。
- `觸控：等待手指離開`：先移開手指，不要繼續往平面壓。

畫在實體表面上的方框和右下角的固定鍵盤會同時變色，兩邊應該一致。

PowerShell 的事件內容會類似：

```text
按鍵事件 按鍵=5 時間戳記毫秒=... 指尖XYZ毫米=(...) 平面UV毫米=(...) 原始距離毫米=... 濾波距離毫米=...
```

## 第九部分：所有操作按鍵

請先點一下主 OpenCV 視窗，確保它取得鍵盤焦點，再按以下按鍵：

| 按鍵 | 功能 | 什麼時候使用 |
| --- | --- | --- |
| `C` | 清除目前校正，準備表面掃描與三點校正。 | 相機、紙張或虛擬鍵盤位置改變時。 |
| `S` | 開啟／隱藏「參數設定」視窗。 | 調整參數或查看距離預覽時。 |
| `Space` | 開始表面掃描，或開始目前校正點的連續取樣。 | `C` 後先掃描；掃描完成後依序用於 P1、P2、P3。 |
| `Enter` | 用三個已記錄校正點計算平面和鍵盤尺寸。 | 三個點都顯示已記錄後使用。 |
| `R` | 清除校正、鍵盤幾何、觸控和濾波暫存狀態。 | 校正做錯、卡住或想完整重來時。 |
| `Q` 或 `Esc` | 離開程式。 | 結束使用時。 |

`Space` 或 `Enter` 在非校正狀態按下不會完成任何數字輸入；本程式沒有系統按鍵注入功能。

## 第十部分：故障排除——照症狀處理

### A. 程式一啟動就關閉

請不要雙擊 EXE。從 PowerShell 重新啟動，讀取最後幾行錯誤。

依序確認：

1. 目前位於 `build\windows-release`。
2. `aerial_touch_app.exe` 存在。
3. `OrbbecSDK.dll` 和 `OrbbecSDKConfig.xml` 存在。
4. `extensions\frameprocessor\ob_frame_processor.dll` 存在。
5. Gemini 2 已接上，且沒有被其他程式使用。

這個 frame processor 是深度串流需要的 runtime；不能只複製 EXE。

### B. 顯示「相機啟動失敗」

先做硬體排除：

1. 拔除再重新插入 Gemini 2。
2. 換到主機板直連的 USB 3.x 連接埠。
3. 拿掉 USB Hub 和過長／鬆動的延長線。
4. 關閉 Orbbec Viewer 或其他相機程式。
5. 重新從 `build\windows-release` 啟動。

如果仍失敗，用相機探測工具確認 SDK：

```powershell
Set-Location -LiteralPath "D:\Claude Project\Air Image\build\windows-release"
.\orbbec_stream_probe.exe
```

它會列出感測器、串流設定，並測試 depth-only、color-only 和 RGB-D。若探測工具也失敗，優先處理 USB、SDK runtime 或裝置連線，不要先重做校正。

### C. 顯示「追蹤：無法使用」

確認目前啟動資料夾有：

```text
mediapipe_hand_bridge.dll
opencv_core4.dll
opencv_imgproc4.dll
z.dll
assets\models\hand_landmarker.task
```

缺少模型時執行：

```powershell
Set-Location -LiteralPath "D:\Claude Project\Air Image"
.\scripts\fetch_hand_model.ps1
```

缺少 bridge 時重新執行：

```powershell
.\scripts\build_mediapipe_bridge.ps1 -MediaPipeSource .\third_party\mediapipe
```

### D. 顯示「追蹤：未取得本幀觀測」

這通常是暫時沒有可靠的手部觀測，不一定是故障。請：

1. 把整隻手放進影像內。
2. 讓食指伸直。
3. 增加光線，但不要讓指尖強烈反光。
4. 讓手指不要被手掌或物體遮住。
5. 停住約一秒，再看是否回到 `本幀有效`。

短暫失追只保留畫面顯示，不會拿暫存位置產生按鍵事件，這是正常的安全行為。

### E. 看到指尖，但沒有 XYZ 或無法開始取樣

影像看得到指尖，不代表深度一定有效。`XYZ` 仍是實際觸控距離的必要資料；校正時則優先使用已掃描表面與指尖像素射線。請：

1. 把指尖移到相機能取得深度的距離。
2. 避免純黑、透明、鏡面或強反光表面。
3. 保持指尖不動。
4. 表面掃描時，確認 `追蹤：本幀有效` 且指尖周圍看得到目標表面後再按 `Space`；三點取樣時，依畫面的「等待有效指尖位置與表面射線」提示調整手勢。

如果畫面顯示「等待有效指尖位置與表面射線後開始收集」，不要連續按鍵；保持位置，讓手指與目標表面同時維持在畫面內。

### F. 校正取樣中斷或進度不動

取樣期間請不要移動手指，也不要再次按 `Space`。如果顯示：

- `校正取樣中斷：相機影像暫時無效，請按空白鍵重試`：檢查 USB 和相機串流，恢復後按 `Space` 重試。
- `校正取樣中斷：追蹤逾時，請按空白鍵重試`：重新把整隻手放回畫面，等 `本幀有效` 後按 `Space`。
- `校正點散布過大或有效樣本不足，請保持手指不動後按空白鍵重試`：把手指放穩，避免手腕晃動，再按 `Space`。

### G. 表面掃描失敗

如果顯示「表面掃描失敗：深度不足、混入不同平面，或掃過的範圍太窄」：

1. **先看狀態列的 `涵蓋範圍` 有沒有達到門檻。** 這是最常見的原因：手指只在小範圍內移動時，取到的樣本會集中在一條窄帶上，平面雖然貼合得很好，傾角卻沒有被決定。請把整個鍵盤區域掃過一遍，尤其四個角落。
2. 確認食指周圍看得到紙張、板面或螢幕表面，不要只讓鏡頭看見手指。
3. 掃描時不要把整隻手掌壓在目標上。程式會排除手部關鍵點附近的像素，但手掌佔掉的面積越大，能取到的表面樣本就越少。
4. 避免鏡面、透明、純黑或強反光的目標表面。
5. 不要跨到桌面、牆面或其他明顯不同深度的背景。
6. 按 `Space` 重新開始掃描；目標位置改變時則按 `C`。

### H. 三點完成但按 Enter 校正失敗

表面基準會被保留，從 P1 重新做即可；只有目標位置變動時才按 `C` 重掃。目前只有三種失敗訊息：

| 訊息 | 意思 | 怎麼修 |
| --- | --- | --- |
| `校正失敗：鍵盤表面方向無法建立` | 三個點太靠近、或幾乎在同一條直線上，撐不出一個平面。 | 把鍵盤範圍畫大一點；P2、P3 距離 P1 都必須超過「校正點最小距離」（預設 80 mm）。確認 P3 真的在下邊界，不是在上邊界附近。 |
| `校正失敗：鍵盤或按鍵尺寸無效` | 算出來的寬或高不是正數，通常是點的順序記錯（例如 P2 記到了 P1 的左邊）。 | 依 P1 → P2 → P3 的順序重來，P1 在左上、P2 在右上。 |
| `校正失敗：P3 未落在鍵盤下緣範圍內` | P3 的左右位置跑到鍵盤外框以外太多。 | 把 P3 放回 `0` 鍵正下方，不要指到紙張右下角或桌面別的地方。 |

重做時逐項檢查：

1. P1 是 `1` 鍵左上角。
2. P2 是 `3` 鍵右上角。
3. P3 是 `0` 鍵正下方的下邊界，不是右下角。
4. 每個點取樣時都保持不動。

### I. 校正完成，但畫出的鍵盤沒有落在實體目標上

先看畫面有沒有 `相機警告：深度內參尺寸與對齊後影像不符`。

- **有這行警告**：對齊後的深度影像尺寸和相機回報的內參對不起來。此時毫米量測與鍵盤投影都會偏移，任何觸控門檻的調整都沒有意義。請改用另一種對齊方式或另一組深度模式／精度／FPS 組合（`S` 視窗的三個相機下拉選單，改完要重新啟動），並重新校正。
- **沒有警告，只是稍微偏**：多半是三個點指得不夠準。鋪上紙本樣板，按 `C` 重新校正一次。

在方框和實體目標對齊之前，不要花時間調觸控門檻。

### J. 校正完成，但 `按鍵` 顯示 `-`

這表示指尖的平面投影沒有落在任何數字鍵內。請先移動手指的左右／上下位置，直到 `按鍵` 顯示目標數字；不要先往下壓。

按鍵之間已經沒有間隙，所以在鍵盤外框內部通常都會對到某個鍵。會顯示 `-` 的位置只有兩種：整個外框以外，或第四列 `0` 左右兩側那兩個沒有按鍵的空格。

### K. `按鍵` 正確，但沒有事件

依序確認：

1. `校正` 顯示完成。
2. `追蹤` 顯示本幀有效。
3. `按鍵` 顯示目標數字，不是 `-`。
4. `觸控` 顯示 `可觸發`。
5. 先把手指移到平面上方，再以明確動作靠近。
6. 如果動作本來就慢，就別急著移開：停在同一個鍵上不動，看 `停留 N/350 ms` 走完，一樣會觸發。

### L. 第一個鍵成功，第二個鍵沒有事件

這通常是還沒有完成 re-arm。把手指移遠，直到 `觸控：可觸發`，再移到下一個鍵並重新靠近。停留在第一個鍵上不會連續觸發，滑到隔壁鍵也只會讓停留計時重新開始。

### M. 設定已儲存，但相機模式沒有立刻改變

這是預期行為。觸控和深度取樣參數可立即套用；深度工作模式、精度和 FPS 必須：

1. 在設定視窗按「套用並儲存」。
2. 關閉主程式。
3. 從 `build\windows-release` 重新啟動。
4. 查看 PowerShell 和主視窗顯示的相機實際設定。

## 第十一部分：正常結束與重新開始

### 結束程式

1. 先把食指移離虛擬平面。
2. 點一下主 OpenCV 視窗。
3. 按 `Q` 或 `Esc`。
4. 等 PowerShell 回到提示字元。

程式會停止相機串流並關閉 OpenCV 視窗。

### 下次重新啟動

每次重新啟動都做以下流程：

```powershell
Set-Location -LiteralPath "D:\Claude Project\Air Image\build\windows-release"
.\aerial_touch_app.exe
```

啟動後重新執行：

```text
C → 沿鍵盤區域移動 → Space 表面掃描 → 自動完成 → P1 Space → P2 Space → P3 Space → Enter
```

其中每次按 `Space` 後都必須等該點顯示 `已記錄`，不能快速連按。按完 `Enter` 之後，記得再看一次畫在實體表面上的方框有沒有對準。

## 第十二部分：目前版本的限制

- 只支援單手追蹤。
- 目前只有 1–9、0 數字鍵盤；第四列左右兩格沒有對應按鍵。
- 按鍵事件只顯示在 OpenCV 視窗和終端機，不會注入 Windows 系統鍵盤。
- 不包含 ASKA3D 或雙手操作。
- 校正表面與鍵盤幾何只保留在本次執行期間，重新啟動後要重新掃描並做三點校正。
- 實機效果會受 USB 穩定性、相機位置、光線、手部距離和校正品質影響。

## 最短成功檢查表

如果你只想確認自己有沒有做對，最後應該全部符合：

```text
[ ] PowerShell 位於 build\windows-release
[ ] aerial_touch_app.exe 可以啟動
[ ] OpenCV 視窗顯示 Gemini 2 影像
[ ] 追蹤顯示本幀有效，且有 XYZ
[ ] C 後依序完成 P1、P2、P3
[ ] 校正顯示完成
[ ] 畫在實體表面上的鍵盤方框與實際目標重合
[ ] 沒有出現深度內參不符的相機警告
[ ] 按鍵顯示目標數字
[ ] 觸控顯示可觸發
[ ] 食指靠近平面（或停留滿 350 ms）後出現最近按鍵和 PowerShell 事件
[ ] 移開手指後才能再次觸發
```
